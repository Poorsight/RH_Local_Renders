// Where the page should look for the render service.
//
// Left empty on purpose: a page served by the service itself already knows where it is.
// The published copy gets this file rewritten on every boot with the current tunnel
// address, so the only thing anyone ever types by hand is the access key.
window.RH_API_BASE = "";
