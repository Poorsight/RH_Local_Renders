{
  "name": "rh-local-renders",
  "version": "2.0.0",
  "description": "Local RH model inspection, BatchRender job generation, and Unreal render control.",
  "private": true,
  "scripts": {
    "build": "node --check app.js && node --check server.cjs",
    "test": "node --test test/local-render.test.cjs",
    "start": "node server.cjs"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
